import { useState } from 'react'

// {{COMPONENTS_IMPORT}}

export default function App() {
  // {{STATE_SETUP}}

  return (
    <div style={{ minHeight: '100vh', padding: '2rem' }}>
      {/* {{HEADER}} */}
      
      <main>
        {/* {{CONTENT}} */}
      </main>

      {/* {{FOOTER}} */}
    </div>
  )
}
